u"ni.scheme:ni.scheme"->create('ni.behavior',
  name        => 'URI scheme representing a behavioral metaclass',
  synopsis    => ' u"ni.behavior:readable"->methods
                 | u"ni.behavior:blocking"->modify(u"ni.scheme:http") ',
  description => q{
    Behaviors are the core mechanism used to associate methods with classes.

    TODO: explain more});
