ni('ni:/object')->child('/fabric/native')
  ->doc
  ->synopsis(q[
    my $n = ni('ni:/fabric/native')
      ->new
      ->defmethod('identity', ['long'], ['long'], q{
        /* todo this is awful */
      })
      ->defmethod('f', 
    print "f(10) = " . $n->f(10) . "\n";
    ])
  ->description(
    q[Compiles a binary that interfaces with Perl to support calls to the
      specified methods. Communication happens over a socket pair.])
  ->end;
