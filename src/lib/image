ni::class->new(name => 'ni::image',
               isa  => ['ni::persistent_object'],
               code => <<'_');
sub run {
  my ($self, $argv, $env) = @_;
  $_->run($argv, $env) for @{$$self{alternatives}};
}

# TODO: implement proper joint dependency logic so we can slice serialization
# on this class and drop it out for terminal images. I think this just amounts
# to slice aggregation, but I'm not sure.
_
