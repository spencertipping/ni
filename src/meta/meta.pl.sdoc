Image-related data sources.
Long options to access ni's internal state.

package ni;

deflong 'meta/self',
  pmap {sh ['ni_append', 'cat'], stdin => read_map 'ni.map'} mr '^//ni/self';

deflong 'meta/map',
  pmap {sh ['ni_append', 'cat'], stdin => $self{'ni.map'}} mr '^//ni/map';
