#!/bin/bash
# Runs a specific benchmark on synthetic input.

mkdir $(dirname $0)/gen
cd $(dirname $0)/gen

nfu --repeat 10 /usr/share/dict/words -f00 > testdata

gcc ../$1.c -o $1
time ./$1 < testdata > /dev/null
time ./$1 < testdata > /dev/null

cd ..
rm -r gen
